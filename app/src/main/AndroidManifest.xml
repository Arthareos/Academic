<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simonedifonzo.academic">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Academic">
        <activity android:name=".helpers.EditCourseActivity"></activity>
        <activity android:name=".helpers.CreateAcademicWorkActivity" />
        <activity android:name=".helpers.CreateCourseActivity" />
        <activity android:name=".helpers.SelectSpecializationActivity" />
        <activity android:name=".helpers.CreateMediaResource" />
        <activity android:name=".helpers.CreateDocumentResource" />
        <activity android:name=".AcademicWorkBrowserActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".CourseBrowserActivity" />
        <activity android:name=".PersonalActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>